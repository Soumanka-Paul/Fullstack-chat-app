{
  "name": "chatapp",
  "version": "1.0.0",
  "description": "MERN Chat Application",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend/chat-app",

    "build": "npm run install-all && npm run build --prefix frontend/chat-app",

    "start": "npm start --prefix backend",

    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend/chat-app\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}